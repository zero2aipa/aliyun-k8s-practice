# 集群通用变量（请按需修改）
# ========== 节点与账号 ==========
CONTROL_PLANE_IP="10.0.1.1"
WORKER_IPS=("10.0.2.1" "10.0.2.1")
ALL_NODES=("$CONTROL_PLANE_IP" "${WORKER_IPS[@]}")

SSH_USER="root"
SSH_PASS="YourRootPassword"     # 用于 sshpass 分发和远程执行
SSH_PORT="22"

# ========== 语言/时区 ==========
LANG_TO_SET="zh_CN.UTF-8"       # 统一系统语言（支持中文显示）
TIMEZONE="Asia/Shanghai"

# ========== 安装与缓存 ==========
PKG_CACHE_DIR="/opt/k8s-pkg-cache"  # 本地离线包目录（会自动创建）
K8S_VERSION=""                      # 例如: 1.30.4-00；留空=最新稳定版
CRICTL_VERSION="v1.30.0"            # 作为 APT 失败时的回退版本

# ========== 镜像与镜像源 ==========
PAUSE_IMAGE="registry.aliyuncs.com/google_containers/pause:3.9"
ALIYUN_K8S_APT="https://mirrors.aliyun.com/kubernetes/apt"
DAOCLOUD_MIRROR="https://docker.m.daocloud.io"
NETEASE_MIRROR="https://hub-mirror.c.163.com"
BAIDU_MIRROR="https://mirror.baidubce.com"
